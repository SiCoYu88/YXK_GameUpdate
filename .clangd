CompileFlags:
  # 抑制编译错误，避免因缺少 UE 引擎头文件路径导致的级联报红
  Add:
    - -Wno-everything
    - -ferror-limit=0

Diagnostics:
  # 关闭所有诊断（因为没有 compile_commands.json，clangd 无法正确解析 UE 代码）
  # Suppress: ['*']
  UnusedIncludes: None
  MissingIncludes: None
